import type {CatalogSnapshot} from "./catalog.ts";
type Storage={get<T>(key:string):Promise<T|undefined>;put<T>(key:string,value:T):Promise<void>};type DurableState={storage:Storage};
export class RaceCatalogCache{private state:DurableState;constructor(state:DurableState){this.state=state}async fetch(request:Request):Promise<Response>{const path=new URL(request.url).pathname;if(path!=="/snapshot")return new Response("Not found",{status:404});if(request.method==="GET"){const snapshot=await this.state.storage.get<CatalogSnapshot>("snapshot");return snapshot?Response.json(snapshot):new Response("Cache is warming",{status:404})}if(request.method==="PUT"){const snapshot=await request.json() as CatalogSnapshot;await this.state.storage.put("snapshot",snapshot);return new Response(null,{status:204})}return new Response("Method not allowed",{status:405})}}
